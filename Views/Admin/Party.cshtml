@model Party
@{
    ViewData["Title"] = "Election Management";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Parties</title>

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <style>
        body {
            background-color: #f8f9fa;
        }

        .form-container {
            max-width: 700px; /* Increased width */
            background: #fff;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
        }

        .btn-primary {
            background-color: #007bff;
            border: none;
        }

            .btn-primary:hover {
                background-color: #0056b3;
            }

        .form-control, .form-select {
            height: 45px;
        }
    </style>
</head>
<body>

    <div class="container vh-100 d-flex justify-content-center align-items-center">
        <div class="form-container">
            <h2 class="mb-4 text-center">Add Party</h2>

            @if (ViewBag.Errors != null)
            {
                <div class="alert alert-danger">
                    <ul>
                        @foreach (var error in ViewBag.Errors)
                        {
                            <li>@error</li>
                        }
                    </ul>
                </div>
            }

            <form method="post" asp-controller="Admin" asp-action="AddParty">
                <div class="row">
                    <div class="col-md-12 mb-3">
                        <label class="form-label">Party Name</label>
                        <input type="text" asp-for="PartyName" class="form-control" required>
                        <span class="text-danger" asp-validation-for="PartyName"></span>
                    </div>

                    <div class="col-md-12 mb-3">
                        <label class="form-label">Leader</label>
                        <input type="text" asp-for="Leader" class="form-control" required>
                        <span class="text-danger" asp-validation-for="Leader"></span>
                    </div>

                    <div class="col-md-12 mb-3">
                        <label class="form-label">Founded Year</label>
                        <input type="number" asp-for="FoundedYear" class="form-control" required>
                        <span class="text-danger" asp-validation-for="FoundedYear"></span>
                    </div>
                    <div class="col-md-12">
                        <button type="submit" class="btn btn-primary w-100">Add Party</button>
                    </div>
                </div>
            </form>
        </div>
    </div>

</body>
</html>
